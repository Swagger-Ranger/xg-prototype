/**
 * MCP transport (HTTP + SSE Streamable) — skeleton only (target: M3.1).
 *
 * <p>M3.1 时在此目录放：</p>
 * <ul>
 *   <li>{@code HttpSseTransport.java}：基于 Spring WebFlux 的 SSE endpoint。</li>
 *   <li>{@code StreamableHttpTransport.java}：MCP 2026 推荐的 Streamable HTTP 模式（替代纯 SSE）。</li>
 * </ul>
 */
package com.xg.mcp.transport;
