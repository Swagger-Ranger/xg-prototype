/**
 * MCP (Model Context Protocol) Server module — skeleton only (target: M3.1).
 *
 * <p>把 xg-business / xg-platform 的业务能力以 MCP 协议暴露，供 Python AI Sidecar、
 * Cursor、学校自有 AI 平台等任意 MCP 客户端调用。</p>
 *
 * <p>当前模块 <strong>未加入</strong> {@code settings.gradle}，不参与构建。</p>
 *
 * <p>启用与设计要点见模块根的 {@code README.md}。</p>
 */
package com.xg.mcp;
