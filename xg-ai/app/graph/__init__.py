"""Custom LangGraph StateGraphs for complex flows — *skeleton only*.

约定：仅当流程比"对话型 ReAct"更复杂（多分支 / HITL / 子图） 时才在本目录写自定义图。
普通会话型 Agent 直接用 `app.agents.*`，不必落到本目录。

STATUS: skeleton-only, target: M3.x
"""
