"""Teacher / counselor conversation agent — *skeleton only*.

预期职责（M3.3）:
- 辅导员 / 教师身份的对话入口；工具集 = 查学生 / 查待审批 / 起草通知 / 异常预警 / 工作日志辅助 ...
- 中等风险动作（如发送通知、批量审批）走 HITL。

STATUS: skeleton-only, target: M3.3
"""
from __future__ import annotations

from typing import Any


def build_teacher_agent(*args: Any, **kwargs: Any) -> Any:
    raise NotImplementedError("build_teacher_agent will be implemented in M3.3")
